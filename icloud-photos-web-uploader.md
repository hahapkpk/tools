# iCloud Photos Web Uploader

Tampermonkey 脚本，为 iCloud 网页版照片添加快速上传、自动转码、滚轮缩放和智能刷新功能。

## 安装

点击下方链接，Tampermonkey 会自动提示安装：

```
https://raw.githubusercontent.com/hahapkpk/tools/main/icloud-photos-web-uploader.user.js
```

## 1.14.2 修复说明

- 已按真实 iCloud Photos 页面菜单结构复现并修复繁体中文识别。
- 识别“加入喜好項目 / 下載 / 更多下載選項 / 隱藏 / 刪除 / 加至相簿”等繁体命令。
- “拷贝图像”现在会插入繁体菜单的“下載”之前。

## 1.14.1 修复说明

- 兼容照片缩略图上的遮罩事件：从事件路径和鼠标命中层识别实际图片。
- 兼容 iCloud 不使用 ARIA `menuitem` 的自定义右键菜单；可通过菜单容器类名识别“下载”项。
- 右键菜单出现后立即插入“拷贝图像”，不再等待图片预读取完成。
- “拷贝图像”保持在“下载”前；复制时才读取图片并在失败时显示原因。

## 1.17.0 更新说明

大图界面的按钮改成图标并支持拖动记忆：

- 由文字按钮改为 **40×40 圆形图标按钮**（拷贝图片图标，浅色底），不再显示文字；悬浮提示与无障碍标签仍是「拷贝大图」。
- **可按住拖动**，松手即记住位置（独立存储键 `icloud-web-uploader-oneup-copy-position`），下次打开大图仍在原位；窗口尺寸变化或换到更小的屏幕时会自动夹回可视区。
- 拖动结束后的那一下点击不会触发拷贝，避免"挪个位置顺手复制一张"。
- 识别是否在大图界面时增加"必须在视口内"过滤：轮播会预挂载左右邻图、大图关闭后也可能留下布局，这些屏幕外的 `<img>` 不再误判。

## 1.16.2 更新说明

修复「拷贝大图」按钮在大图界面根本不出现：

- 之前用 iCloud 的组件名（`OneUpCarouselItem` 之类）判断"是否在大图"，但这些名字既不一定是标签名、也不在应用 bundle 里作为稳定字符串存在，导致匹配永远失败。现在改为**按尺寸特征识别**：自然尺寸 ≥800px、屏幕上 ≥500px、且不在网格格子（`.grid-item`）内的 `<img>` 才算大图。网格缩略图渲染约 160px、派生图 480px，天然被排除。
- 判断方式从"等一次 DOM 变化"改为每 700ms 轮询，避免图片解码完成时没有新 DOM 变化、按钮迟迟不出现。

## 1.16.1 更新说明

「拷贝大图」按钮更好找：

- 改成浅色胶囊按钮（白底深字、带阴影）。原来深色半透明压在 OneUp 的深色全屏背景上，容易被当成禁用状态而忽略。
- 第一次打开大图时会提示一次「点左下角『拷贝大图』可复制高清大图」，之后不再打扰。

注意：按钮出现在**点开照片后的大图界面**（左下角）；网格页没有这个按钮，因为网格里无法确定"右键的那张"对应哪条记录。

## 1.16.0 更新说明

新增「拷贝大图」：把点开大图（OneUp）后加载的那张高清图（通常 2000px 上下）写进剪贴板，质量远好于网格缩略图（480px）。

- 大图界面左下角会出现「拷贝大图」按钮，点一下即复制，并提示实际像素尺寸。
- `Ctrl+C` / `Alt+C` 在大图界面不再要求鼠标正好悬停在照片上：指针停在工具栏等位置时会自动回退到当前居中的大图。
- 为什么不做在网格右键里“自动打开大图再拷”：实测 iCloud 只认真实点击才会打开大图（单击/双击、点在图片或格子上四种合成事件都不触发），网页脚本无法代替这一步；所以改为你点开照片后一键复制。

## 1.15.4 更新说明

- 网格右键菜单去掉“拷贝图像”行，只保留“下载原片”。原因：网格里能拿到的只有当前渲染的小派生图，而实测无法把某张缩略图对应到 CloudKit 记录（网格显示顺序与按拍摄日期/按加入日期两套索引都不一致），iCloud 自身的下载又在 worker 中执行、主线程无法截获其数据，所以“右键把原片复制进剪贴板”在当前网页端做不到；留着那一行只会让人以为拷到的是原图。
- 注入逻辑因此简化：不再按坐标解析照片、也不再预取缩略图，只在新出现的照片右键菜单里补一行“下载原片”。需要原片时用它（多选时为 zip）。

## 1.15.3 更新说明

- 修复注入行“发灰、没图标”：改为克隆 iCloud 原生菜单行（`ui-menu-item.menuItem`）后只替换文字与图标，因此字体、颜色、图标、悬停效果都与原生行一致。
- 拷贝成功后的提示会带上实际像素尺寸；当尺寸明显偏小时追加“原片请用「下载原片」”，避免误以为拷到的是原图。
- 说明为什么菜单里的“拷贝图像”不是原片：网格 DOM 与 React 层都不暴露资源标识，实测网格显示顺序与 CloudKit 索引顺序不一致（无法定位“右键的那张”对应的记录），iCloud 自身的下载又在 worker 中执行、主线程无法截获其数据。因此原片只能通过“下载原片”以文件形式获得。

## 1.15.2 更新说明

- 网格右键菜单新增“下载原片”：转交 iCloud 自己的“下载”命令执行，因此拿到的是真正的原片；多选照片时同样转交（iCloud 会打包成 zip），适合往资源管理器 / 微信批量送文件的场景。
- 修复复用弹层时的错拷：右键菜单节点会被 iCloud 反复复用，之前“拷贝图像”行会一直引用第一次打开时的照片；现在每次打开都会改绑到当前鼠标下的照片。
- 已知限制：右键的“拷贝图像”拷贝的是当前网格里已渲染的那张图（缩略图分辨率），不是原始文件。网格 DOM 与 React 层都不暴露资源标识，无法把某张缩略图可靠对应到 CloudKit 记录，所以原片走“下载原片”。需要原片级粘贴时请用“下载原片”，或先点开大图再右键。

## 1.14.0 更新说明

- 在多图片浏览页中，右键照片后会在 iCloud 原有菜单的“下载”前增加“拷贝图像”。
- 命令读取当前已渲染图片的二进制数据并写入系统剪贴板；不会打开照片详情页。
- 已含“拷贝图像”或 “Copy Image”的单图预览菜单保持 iCloud 原生菜单，不重复添加命令。
- 若浏览器未开放图片剪贴板能力或图片读取失败，悬浮球会显示失败原因。

升级不需要迁移配置。

## 1.13.2 更新说明

本版本重点修复连续上传、图库刷新、图片切换缩放和上传控件识别问题：

- 连续滚轮缩放改用当前平移量计算鼠标锚点，连续放大时图片不再漂移。
- 多批次上传按队列统一管理刷新需求；前一批成功、后一批失败时，不会丢失前一批的自动刷新。
- 自动刷新现在验证两段路由切换是否真的改变了 Photos 图库根节点、网格或活动侧边栏；普通缩略图加载和样式变化不会误报成功。
- 新上传批次会中止旧的 CloudKit 查询；同步状态请求使用 `no-store`，并比较全部 zone，而不是依赖第一个 zone。
- 文件控件识别排除禁用、非图片和低置信度的 `input[type="file"]`；点击上传入口后优先选择合格的新控件，路由变化后自动废弃旧缓存。
- 切换照片时监听 `src`、`srcset` 和 `load`，自动清除上一张照片的缩放；清理过程不会覆盖 iCloud 为新照片写入的内联样式。
- 删除旧版面板尺寸调整代码，降低维护复杂度。

升级不需要迁移配置。已保存的悬浮球位置继续保留；安装地址仍为上方 raw 链接。

验证结果：脚本语法检查通过，50 项自动化测试全部通过，并完成 Chromium 中的连续缩放、跨批次上传刷新、软刷新判定和换图样式保护验证。

## 功能

### 📤 快速上传

- **粘贴上传**：页面任意位置 Ctrl+V 粘贴截图/图片直接上传
- **拖拽上传**：拖拽图片文件到悬浮球上传
- **点击选择**：点击悬浮球打开文件选择器
- 右键悬浮球：运行上传控件检测诊断

### 🔄 自动格式转码

iCloud 网页版只接受 JPEG，脚本自动将其他格式转为 JPEG：

| 格式 | 支持 | 说明 |
|------|------|------|
| JPEG | ✅ 直接上传 | 不转码 |
| PNG, WebP, GIF, BMP, AVIF, APNG | ✅ 自动转码 | 浏览器原生解码 |
| SVG, ICO | ✅ 自动转码 | SVG 无尺寸时默认 1024×1024 |
| HEIC, HEIF, TIFF | ⚠️ 接受但可能失败 | Windows Chrome 无解码器 |

转码细节：
- 透明背景填白色
- JPEG 质量 0.92
- 文件名自动改为 `.jpg`
- 长边超过 8192px 或总像素超过 4000 万时自动等比缩小，并明确提示缩放前后尺寸
- 保留源文件的 `lastModified` 时间；单张转换失败只跳过该张并列出原因

### 🔍 滚轮缩放 + 拖拽平移

- **滚轮缩放**：鼠标悬在预览图上滚轮缩放 1x～8x
- **拖拽平移**：缩放后按住左键拖动，平移范围受边界限制，图片不会被拖出视野
- **Esc / 0 / 双击**：立即复位
- 缩放锚点跟随鼠标位置
- 模仿 iCloud 原生缩放机制（transform 加在 `OneUpCarouselItem-itemWrapper` 上）
- 缩放只在指针位于预览图内时生效，不会劫持整页滚动

### ⚡ 智能刷新

上传后自动检测服务器处理状态：

1. 向 iCloud 交接文件前记录全部 CloudKit zone 的同步状态，避免错过快速变化
2. 文件交接后每隔 2～5 秒以 `no-store` 请求轮询 `zones/list` API
3. 任一 zone 状态变化 = 服务器已处理 → 自动 hash 路由切换刷新图库
4. 往返两程都须观察到图库根节点/网格替换或活动侧边栏变化，普通缩略图加载和样式抖动不算成功
5. 轮询以总计 30 秒为截止时间，不会因退避累计到约 54 秒
6. 超时或无法读取同步状态时只尝试软刷新，不再强制整页重载
7. 新批次会中止旧请求但保留待刷新状态，队列结束后继续检测；等待期间点击悬浮球可立即软刷新

### 🎯 悬浮球 UI

- 44×44 圆形 FAB，右下角
- 可拖动，位置自动记忆
- 上传中：灰色 + 旋转光环
- 等待刷新：绿色 + 刷新图标
- 状态 toast 自动消失；长错误列表可滚动，不再被裁切

## 技术实现

### 架构

脚本注入到 iCloud Photos 的内部 iframe（`/applications/photos3/current/<locale>/index.html`），不在外层 shell 页面。

```
https://www.icloud.com.cn/photos/          ← 外层 shell（不注入面板）
  └ iframe /applications/photos3/...       ← 真正的 Photos App（注入面板）
```

### 关键 DOM 结构（通过 chrome-devtools-mcp 逆向）

```
UI-MAIN-PANE
  └ PhotosRootContent
    └ NavigationPageContainer (overflow:hidden)
      └ OneUp (position:fixed, 全屏)
        └ ReactSwipeCarousel (overflow:hidden)
          └ OneUpCarouselItem.is-center (overflow:hidden)
            └ OneUpCarouselItem-itemWrapper (overflow:hidden, iCloud 在此加 transform 缩放)
              └ ProgressiveImageElement
                └ IMG
```

### iCloud 原生缩放原理

iCloud 的缩放滑块（`ToolbarOneUpZoomSlider`）通过在 `OneUpCarouselItem-itemWrapper` 上设置 `transform: matrix(scale, 0, 0, scale, tx, 0)` 实现缩放。容器的 `width/height/left/top` 保持不变，`overflow:hidden` 不影响（因为是容器自身在缩放）。

本脚本完全模仿此行为。

### iCloud 照片刷新机制

- **Push 通知**：shared worker 通过 WebSocket 连接 `push.apple.com`
- **Sync Token**：CloudKit `zones/list` API 返回当前同步状态
- **数据拉取**：`records/query` 获取照片列表
- **路由刷新**：hash 切换（如 `#/recents` → 回原 hash）触发 React 组件重挂载

### 粘贴事件处理

- 每个文档复用同一粘贴处理器，并用 `WeakSet` 保证每个具体事件目标只注册一次
- 在 `window` / `document` / `body` 三层 capture 阶段注册
- 用 `WeakSet` 做事件级去重，不修改浏览器原生 Event 对象
- 跳过 input/textarea/contentEditable 目标

### 失败降级

脚本可能遇到的每一次降级都会显式提示，不再“静默成功”：

| 场景 | 行为 |
|------|------|
| 单张图片无法解码 | 跳过该张并列出原因，其余继续上传 |
| 解码/编码卡住 | 解码总计 20s、编码 30s 后按失败处理，并释放可释放的位图资源 |
| `input.files` 赋值或事件派发被浏览器拒绝 | 提示改用“选择图片”手动选择，不泄漏异步异常 |
| 找不到文件输入框 | 只向一个候选目标发送拖放事件；仅页面取消 `dragover`/`drop` 时报告已接受 |
| 无法验证软刷新已改变图库 | 保留待刷新状态并提示手动切换视图；可点击悬浮球重试，不整页重载 |
| 路由被 Apple 改名 | 用 DOM 标记兜底挂载；外壳页会尝试接管已就绪的 iframe |

## 开发

### 运行测试

```bash
cd tools
node --test tests/icloud-photos-web-uploader.test.js
```

### Chrome DevTools 调试

使用 `chrome-devtools-mcp` 连接已打开的 Chrome 进行实时 DOM 分析：

```bash
# 1. 用调试模式启动 Chrome（桌面有快捷方式）
#    参数：--remote-debugging-port=9222 --user-data-dir=%LOCALAPPDATA%\Google\Chrome\User Data

# 2. 启动 MCP 守护进程
chrome-devtools start --browser-url http://127.0.0.1:9222

# 3. 操作
chrome-devtools list_pages
chrome-devtools select_page 1
chrome-devtools evaluate_script "() => { return document.title; }"
chrome-devtools take_snapshot
chrome-devtools click <uid>
```

### 直接 CDP 连接

```javascript
// 不依赖 MCP，直接用 WebSocket 连接 Chrome
const http = require('http');
const { WebSocket } = require('ws');
http.get('http://127.0.0.1:9222/json', res => {
  let d = ''; res.on('data', c => d += c);
  res.on('end', () => {
    const tab = JSON.parse(d).find(t => t.url.includes('icloud'));
    const ws = new WebSocket(tab.webSocketDebuggerUrl);
    ws.on('open', () => {
      ws.send(JSON.stringify({id:1, method:'Runtime.evaluate', params:{expression:'document.title', returnByValue:true}}));
    });
    ws.on('message', data => console.log(JSON.parse(data)));
  });
});
```

## 版本历史

| 版本 | 主要变更 |
|------|----------|
| 1.17.0 | 大图按钮改为 40×40 拷贝图标（无文字）；支持拖动并记住位置；大图识别增加“必须在视口内”过滤 |
| 1.16.2 | 修复「拷贝大图」按钮不出现：改为按尺寸特征识别大图（不依赖 iCloud 组件类名），并改为轮询检测 |
| 1.16.1 | 「拷贝大图」按钮改为浅色胶囊（白底深字+阴影）更好找；首次打开大图提示一次按钮位置 |
| 1.16.0 | 新增大图界面「拷贝大图」按钮；`Ctrl+C` 在大图里不再依赖鼠标悬停位置（回退到居中大图） |
| 1.15.4 | 网格右键菜单移除“拷贝图像”（只能拷到小派生图，易误解），只保留“下载原片”；注入逻辑不再解析照片/预取缩略图 |
| 1.15.3 | 注入的菜单行改为克隆 iCloud 原生行（带图标、同色同字体，不再发灰）；拷贝提示带上实际像素尺寸并在偏小时提示改用“下载原片” |
| 1.15.2 | 网格右键菜单新增“下载原片”，转交 iCloud 原生下载（原片，多选时为 zip）；修复复用弹层时“拷贝图像”错拷第一张照片的问题 |
| 1.15.1 | 修复网格“拷贝图像”只能用脚本派发事件触发、真实鼠标点不动：注入行改插到 `ui-popover-content` 内可交互的菜单列表（外层 popover 为 `pointer-events:none`）并显式声明 `pointer-events:auto`；按行高加高弹层，避免最后一项被挤进滚动区；补充回归测试 |
| 1.15.0 | 快捷键 Ctrl+C / Alt+C 拷贝鼠标下的照片；按坐标从容器解析照片（iCloud 给缩略图设了 `pointer-events:none`）；菜单项等二进制就绪后再启用 |
| 1.14.1 | 修复网格页“拷贝图像”未出现：兼容遮罩事件与自定义菜单容器，菜单出现即插入命令并保持在“下载”前 |
| 1.14.0 | 多图片浏览页右键菜单新增“拷贝图像”；复制当前渲染图片到系统剪贴板；单图预览已有原生命令时不重复添加；补齐拷贝行为回归测试 |
| 1.13.2 | 修复连续滚轮缩放锚点漂移、成功批次后跟失败批次时刷新丢失；软刷新须验证两程语义变化；强化文件输入资格与新增控件选择；CloudKit 比较全部 zone、禁用缓存并中止旧请求；图片来源变化时复位且不覆盖新照片的宿主样式；移除旧面板遗留代码并新增回归测试 |
| 1.13.1 | 修复文件选择后 `FileList` 被清空、侧边栏滚轮误缩放、8x 平移越界、粘贴监听累积、刷新跨批次竞态、拖放广播与误报、异步异常泄漏；解码改为总计 20s、增加 4000 万像素上限、保留原生 transform、DOM 空闲时停止缩放看护、取消破坏性整页重载；33 项测试通过 |
| 1.13.0 | 稳定性修复：缩放不再劫持整页滚动、平移加边界、粘贴监听幂等、drop 不再重复入队、单张转换失败不再中断整批、解码/编码加超时、画布尺寸保护、刷新超时改为软刷新；补充 `tests/icloud-photos-web-uploader.test.js` |
| 1.12.0 | 智能刷新：轮询 CloudKit sync token 替代盲等计时器 |
| 1.11.0 | 缩放 transform 加在 wrapper 上（模仿 iCloud 原生） |
| 1.10.x | 修复缩放遮挡 UI、红色矩形、图片跑偏等问题 |
| 1.9.0 | 软刷新改用 hash 路由切换 |
| 1.8.x | 滚轮缩放 + 拖拽平移 + rAF 看护 |
| 1.7.x | 粘贴去重、保留当前视图、模拟点击 |
| 1.5.0 | 悬浮球 FAB 重设计 |
| 1.4.0 | 注入到 iframe 内部修复核心 bug |
| 1.3.0 | 初始版本 |

## 许可

MIT

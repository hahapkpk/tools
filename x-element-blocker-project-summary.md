# X Element Blocker 项目总结

更新时间：2026-04-26  
当前脚本：`x-element-blocker.user.js`  
当前版本：`1.5.2`  
当前提交：`643ae0ba18fd3408eaf36f9d303cd4870e1370dc`

## 项目定位

X Element Blocker 是一个运行在 Tampermonkey/油猴中的 X.com/Twitter 页面元素屏蔽脚本。它的目标是让用户可以像 uBlock 自定义屏蔽一样，通过点选页面元素来生成规则，并在后续访问 X 页面时自动隐藏这些区域。

脚本匹配范围：

- `https://x.com/*`
- `https://twitter.com/*`

核心使用场景：

- 屏蔽 X 页面侧边栏、Premium 推广、推荐模块、趋势模块等干扰内容
- 临时恢复或调试被误屏蔽的内容
- 管理、备份、导入导出自定义屏蔽规则

## 当前功能

### 元素点选和规则保存

用户可以打开面板后进入“点击选取元素”模式，直接在页面上点选要屏蔽的区域。脚本会生成 CSS selector，并加入待确认列表。

保存规则时支持选择作用范围：

- 全站
- 当前路径
- 当前主页

这样可以避免某条规则影响整个 X 网站。例如只想屏蔽某个页面上的元素时，应该选择“当前路径”。

### 精确 selector 选择

从 `1.5.2` 开始，脚本不再简单保存 `div:nth-of-type(...)` 这类结构路径，而是会生成多个候选 selector 并评分。

候选来源包括：

- `data-testid`
- `aria-label`
- `role`
- `href`
- 当前元素和上层元素的结构路径

评分会考虑：

- 当前命中数量
- 是否使用稳定属性
- 是否为结构选择器
- 是否命中过多
- selector 是否过长或过宽

如果最佳候选仍然有误屏蔽风险，脚本会弹出候选列表，让用户手动选择更精确的一条。

### 规则管理

每条规则现在保存为对象结构，不再只是 selector 字符串。

字段包括：

- `selector`
- `note`
- `scope`
- `enabled`
- `createdAt`
- `updatedAt`

面板中可以对规则执行：

- 启用/禁用
- 删除
- 编辑备注和作用范围
- 单条规则预览
- 定位第一处命中元素
- 查看当前命中数量
- 查看风险标签

旧版字符串规则会自动迁移成新版对象结构，旧规则的添加时间会使用首次运行新版脚本时的时间。

### 搜索和过滤

规则列表支持搜索和过滤。

搜索范围：

- selector
- 备注
- 作用范围

过滤模式：

- 全部规则
- 已启用
- 已禁用
- 当前命中
- 风险提示

### 风险提示

保存规则前，脚本会检查规则是否可能误屏蔽。

目前提示类型包括：

- 命中过多
- 范围过宽
- 结构选择器
- 稳定性较低

如果待保存规则有风险，会弹出确认框，用户可以取消保存并重新选择更精确的 selector。

### 调试和恢复

调试模式不会隐藏元素，而是高亮当前规则命中的元素，并显示规则编号浮标。

可用的调试方式：

- 面板“调试”按钮
- 油猴菜单“开启/关闭调试高亮”
- `Alt+D`
- 单条规则的定位按钮 `⌖`
- 单条规则的预览按钮 `◉`

误屏蔽时可以使用：

- 暂停屏蔽
- 禁用单条规则
- 删除单条规则
- 撤销最近添加
- 恢复最近自动备份

### 备份和恢复

脚本会在关键操作前自动保存最近 5 次规则备份。

会触发备份的操作包括：

- 保存新规则
- 撤销最近添加
- 编辑规则
- 启用/禁用规则
- 删除规则
- 导入规则
- 恢复备份前

面板中可以点击“恢复备份”回到最近一次自动备份状态。

### 导入导出

规则支持 JSON 导出和导入。

导出文件名：

`x-element-blocker-rules.json`

导入时兼容旧版字符串数组和新版对象数组。

### 快捷键

当前快捷键：

- `Alt+B`：打开/关闭面板
- `Alt+P`：暂停/恢复屏蔽
- `Alt+D`：开启/关闭调试高亮
- `Esc`：退出选取模式、关闭单条预览，或关闭面板

快捷键不会在输入框、文本区域、下拉框或可编辑区域中触发，避免影响搜索和编辑操作。

## 性能处理

X 页面是高频动态页面，DOM 变化非常频繁。早期 `1.4.0` 中曾出现页面卡死问题，原因是每次 DOM 变化都会恢复所有隐藏元素、重新查询和隐藏所有命中元素，并可能触发面板重绘。

后续修复策略：

- `MutationObserver` 使用防抖
- 常规 DOM 变化只做增量隐藏
- 忽略脚本面板自身 DOM 变化
- 只有暂停、调试、启用/禁用、删除、导入等状态切换时才做全量恢复重算

这个设计避免了脚本在 X 的动态时间线中反复触发自身更新。

## 数据存储

脚本使用 Tampermonkey 的 `GM_getValue` 和 `GM_setValue` 存储数据。

主要 key：

- `x_element_blocker_rules`：规则列表
- `x_element_blocker_paused`：暂停状态
- `x_element_blocker_debug`：调试状态
- `x_element_blocker_backups`：最近 5 次自动备份
- `x_element_blocker_last_added`：最近一次添加的规则

## 版本演进

重要提交：

- `6881a11`：初始 X Element Blocker 用户脚本
- `532c841`：增加规则导入导出
- `4fe8ce0`：修复 FAB 隐藏样式
- `e219985`：增加规则日期、启用/禁用/删除、暂停和调试能力
- `5145e56`：修复页面卡死问题
- `5290e7d`：优化面板 UI
- `d1a6772`：增加备注、范围、备份、搜索过滤、风险提示等高级规则管理能力
- `8bf0e58`：增加快捷键
- `643ae0b`：提升 selector 精确度

## 使用建议

如果弹出“当前选择器可能不够精确”，建议优先选择：

- 命中数量少的候选
- 带 `data-testid`、`aria-label`、`href` 的候选
- 没有“结构选择器”“稳定性较低”“命中过多”提示的候选

尽量避免保存：

- 很长的 `div > div > div`
- 大量 `nth-of-type`
- 命中数量很高的 selector
- 直接命中 `main`、`article`、`section`、`aside` 等大容器的 selector

误屏蔽时推荐处理顺序：

1. 按 `Alt+P` 暂停屏蔽，先恢复页面。
2. 按 `Alt+B` 打开面板。
3. 用“当前命中”或“风险提示”过滤规则。
4. 对可疑规则点 `◉` 单独预览，或点 `⌖` 调试定位。
5. 禁用、删除，或使用“恢复备份”回退。

## 后续可优化方向

可以继续考虑：

- 自定义快捷键配置
- 更友好的备注编辑弹窗，替代原生 `prompt`
- 规则分组，例如“侧边栏”“广告”“推荐内容”
- 为候选 selector 做更直观的可视化选择面板
- 支持“只在首页/搜索页/帖子页生效”的预设范围
- 为规则添加命中历史统计
- 增加“一键导出完整配置”，包含规则、备份和状态

